# 整合修复分支恢复说明

临时工作目录被系统清理后，本分支从已保存的 `registry-integrated-release-20260914.zip` 恢复。原本地提交 `2f0a341ce670d67b79f1f53902c5e1a06cac1ec1` 未推送成功，远端不存在该对象；本次会产生新的提交。

## 恢复与验证范围

- 58 个前端文件全部与原发布清单的 SHA-256 相同，包括已批准首页、团队权限、随访、注册付款、冻结导出、分析脚本和使用说明。
- 38 份规范迁移全部与原迁移清单相同。五份新增迁移为 0032–0036；历史 0025 的安装修正从分批 SQL 中恢复并核对哈希。
- 包内三份发布/回退程序与原文件逐字节一致；数据库核验工具与契约也已恢复。
- 原测试源码和部分开发构建工具不在离线发布包中，未能全部恢复。`releases/registry-integrated-20260914/validation.log` 是原先的测试记录，不是本次重新执行 86+69 项测试的声明。

本分支补建了可重跑的恢复验收：

```bash
npm ci --ignore-scripts
npm test
python3 scripts/build_registry_integrated.py /tmp/registry-recovered.pyz
```

`npm test` 检查 58 个前端文件、38 份迁移、配置及发布工具哈希和 JavaScript/Python 语法。随后使用真实 PGlite/pgcrypto 与合成 Auth/Storage 结构执行全部迁移，并验证九组行为：成员角色、临床 RLS、跨项目隔离、配额和记录归属保护、冻结导出与校验、禁止自授付费权益、撤权与链接失效、匿名管理拒绝、只读结构核对。

重建器仅接受原审查清单中的代码字节，不会为未审查变动自动更新清单。后续功能修改需要新版本审查与清单。原生浏览器验收未在本次执行；原隔离保护脚本未完整恢复，因此 `test:e2e` 暂时明确拒绝运行，待补回并配置隔离环境后启用。

## 上线状态

本分支用于草稿 PR 和审阅，没有执行真实数据库迁移，也没有修改阿里云上的五个网站。Netlify 自动构建已在仓库配置中阻止，避免绕过数据库前置核验。

先对实际登记站项目 `etsyglgpiutflethgirs` 运行 `scripts/db/production_inventory.sql`，取得结构信息、真实区域及可恢复备份，再验证升级路线。不要直接照历史快速安装说明重跑全部 SQL。后续运维步骤见 `REGISTRY_INTEGRATED_RELEASE.md`；其中旧开发构建命令以本恢复说明为准。

原离线包、恢复源码以及新构建包均不意味着生产已通过验收。原测试基础设施的完整补回、实际 Supabase 集成、真实多连接并发和生产恢复演练仍待完成，故本次保持草稿 PR。
