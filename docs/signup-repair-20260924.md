# 登记站注册修复（2026-09-24）

## 已核实的故障

- 线上 `/signup?trial=1` 与修复前仓库 `site/signup.html` 完全一致，SHA-256 为 `c6fd9df8042f61afd9c809e3521fc74af628cfa5c9388a88d454cd3661cb56f0`。
- 未登录访问会在绑定尚未插入 DOM 的新手引导按钮时抛出空元素错误；注册按钮在此之前已绑定，因此不能将此错误单独认定为所有注册失败的原因。
- 实际密码规则要求至少 8 位，包含大写、小写、数字和特殊字符；原页面提示不完整，校验失败提示只短暂显示。校验失败也会消耗提交次数。
- 引导调用的 `upsert_user_profile`、`create_project`、`create_patient_baseline` 不匹配当前后端。改为复用现有工作台的 `upsert_my_profile` 及 `projects` / `patients_baseline` 插入接口。

## 连接与权限

当前公开配置与工作台实际接口相容。核查确认相关表 RLS 已开启，资料 RPC 有相应调用权限。项目创建者、试用期和患者写入限制继续由数据库控制。本次不改数据库连接、数据库结构、权限、RLS、用户密码或其他站点。

这次检查不能证明截图中用户失败的唯一原因。未代用户注册、发送邮件或修改账号。

## 验证与发布

离线注册流程回归：`npm run test:signup`，13 项通过，覆盖首次加载、密码规则、重复提交、慢请求、初始化错误/超时、邮箱验证和引导保存。测试执行真实页面模块及密码/限流代码，接口使用模拟响应，没有联网创建账号。

部署工具回归：`python3 -m unittest discover -s tests -p 'test_deploy_signup_fix.py'`。

注意：仓库原有 `npm test` 默认指向生产网站，不作为本次离线回归命令。

发布包仅替换 `signup.html`。安装前校验原文件和载荷 SHA-256，保存站点目录外的备份，然后原子替换。不能确定 Nginx 站点根目录或原文件已变化时停止，不猜测其他网站目录。无需重启服务。

当前工作环境没有阿里云服务器登录连接；GitHub 分支/PR 和本地测试完成不等于线上已部署。需在登记站服务器安装修复包后，再验收浏览器注册、真实邮件确认（如已开启）、登录和新手引导。线上最终验收由使用自己邮箱及密码的测试人员完成。
